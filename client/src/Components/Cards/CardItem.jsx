import { useDispatch } from "react-redux";

const CartItem = ({ cartItem }) => {
  //   const { id, name, image, stock, qty, price } = cartItem;
  //   const dispatch = useDispatch();

  return <>hello</>;
};

export default CartItem;
